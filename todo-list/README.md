The Interactive To-Do List

    What to do: Create an input box and an "Add" button. When submitted, append the text to an array state and render the items as a list. Add a "Delete" button next to each item that removes only that specific item from the list when clicked.

    The Goal: Learn how to filter an item out of an array state using its unique ID or index.