# Project 1

The Step Counter

    What to do: Create a number state that starts at 0. Add a text input where you can type a step size (e.g., 5). Add a button that adds that step size to your total count every time it's clicked.

    The Goal: Learn how to convert string input values into integers before updating state.