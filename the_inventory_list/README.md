The Inventory List (Static Array)

What to do:

    Create a state variable that holds a fixed array of items (for example, three  items like ["Iron", "Gold", "Diamond"]).

    Use the JavaScript .map() function to loop through that array and print each item as a list point on your screen.

    The Goal: Get comfortable looping through arrays and rendering multiple elements dynamically without hardcoding them.