# The Live Text Mirror

What to do:

    Place an empty text input box (<input type="text" />) on the screen.

    Right below it, add a paragraph tag (<p>).

    Make it so that whatever you type into the input field instantly mirrors and displays inside that paragraph tag in real time as you type.

    The Goal: Learn how to capture input change events (onChange) and save a string text into React's memory.